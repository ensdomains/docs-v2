import { WIP } from '@/components/wip/WIP';

{/** @type {import('@/lib/mdxPageProps').MdxMetaProps} */}
export const meta = {
    description: '',
    emoji: '⚙️',
    contributors: []
};

# Naming Contracts

<WIP
    missing={[
        'The one-liner for letting your contract set its primary name',
        'Reasons why your contract should have a name',
        'How contract names show up in dApps & transaction history',
    ]}
/>
