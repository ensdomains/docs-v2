import { WIP } from '@/components/wip/WIP';

{/** @type {import('@/lib/mdxPageProps').MdxMetaProps} */}
export const meta = {
    description: '',
    emoji: '✍️',
    contributors: []
};

# Authoring a Registry

<WIP />

<Note>
This document contains **highly specialized** content and is only intended for those who are authoring a Registry.
Such as the ETHRegistrar, DNSRegistrar, or a registrar written by a TLD owner.
</Note>
