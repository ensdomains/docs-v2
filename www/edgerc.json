{
    "config": {
        "routing": {
            "trailing_slash": "never"
        },
        "redirects": [
            {
                "pattern": "^/(.*)$",
                "destination": "https://docs.ens.domains/$1"
            }
        ]
    }
}
