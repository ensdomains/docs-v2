import { Libraries, ensLibraries } from '@/components/Libraries'

export const description =
  'In this guide, we’ll look at how authentication works. Protocol offers two ways to authenticate your API requests: Basic authentication and OAuth2 with a token.'

export const sections = ensLibraries.map((library) => ({
  title: library.name,
  id: library.name.toLowerCase().replace('-', '').replace('.', ''),
}))

# Libraries

There are many ways you can interface with the Ethereum smart contracts. {{ className: 'lead' }}

<Libraries />
